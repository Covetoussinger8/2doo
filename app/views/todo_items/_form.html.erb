<%= form_for([@todo_list, @todo_list.todo_items.build]) do |form| %>
    <%= form.text_field :content, placeholder: "Nova tarefa" %>
    <%= form.submit "Criar nova tarefa", class: "btn btn-primary" %>
<% end %>